<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <context-param>
        <param-name>avatarDir</param-name>
        <param-value>avatars</param-value>
    </context-param>

    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>

    <welcome-file-list>
        <welcome-file>index.xhtml</welcome-file>
    </welcome-file-list>

    <!-- Strony wymagające zalogowania (admin lub user) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Protected JSF pages</web-resource-name>

            <!-- tutaj wpisujesz swoje zabezpieczone widoki -->
            <url-pattern>/vehicles.xhtml</url-pattern>
            <url-pattern>/vehicle.xhtml</url-pattern>
            <url-pattern>/rental-form.xhtml</url-pattern>
            <url-pattern>/rental.xhtml</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
            <role-name>user</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Deklaracje ról (spójne z @DeclareRoles/@RolesAllowed) -->
    <security-role>
        <role-name>admin</role-name>
    </security-role>

    <security-role>
        <role-name>user</role-name>
    </security-role>
</web-app>
